# See https://wiki.hyprland.org/Configuring/Monitors/
#monitor=,preferred,auto,auto

# Just make sure the local screen is on
monitor=eDP-1,1920x1080,2560x1440,1

# All screens setup
# monitor=eDP-1,1920x1080,2560x1440,1
# monitor=DP-4,1920x1080,0x0,1  # Dell left
# monitor=DP-5,2560x1440,1920x0,1  # Acer
# monitor=DP-2,2560x1440,4480x0,1  # Dell right

# Triple-head setup
# monitor=eDP-1,disabled
# monitor=DP-4,1920x1080,0x0,1  # Dell left
# monitor=DP-5,2560x1440,1920x0,1  # Acer
# monitor=DP-2,2560x1440,4480x0,1  # Dell right


# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
# exec-once = waybar & hyprpaper & firefox
#exec-once = kanshi & waybar & dunst
exec-once = waybar
exec-once = dunst

# OSD for brightness/volume controls
exec-once = swayosd-server

exec-once = wl-clip-persist -c both  # Persist clipboard-entries even after application exits
# Disabled as it writes all content of the clipboard (including passwords!) to disk.
#exec-once = wl-paste --type text --watch cliphist store # Clipboard-history, only for text -> SUPER+V

# Tray applications
exec-once = gtk-launch nm-applet.desktop
exec-once = blueman-applet
exec-once = gtk-launch com.seafile.Client.desktop
exec-once = vorta

# Screen setup
exec-once = kanshi

# Wallpaper daemon
exec-once = swww init

# Enable use of hyprctl from within tmux
# https://www.reddit.com/r/hyprland/comments/1bvbyz8/comment/kxzo875/
exec-once = tmux setenv -g HYPRLAND_INSTANCE_SIGNATURE "$HYPRLAND_INSTANCE_SIGNATURE"

# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf

# Some default env vars.
env = XCURSOR_SIZE,24


# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = us, us
    kb_variant = , intl
    kb_model =
    kb_options = compose:altgr
    kb_rules =

    follow_mouse = 1
    # follow_mouse = 2

    touchpad {
        natural_scroll = yes
        scroll_factor = 0.5
    }

    #sensitivity=1.0 # -1.0 - 1.0, 0 means no modification.
    # force_no_accel = true
    # sensitivity=0.4
    # accel_profile=flat
    
    # Flat
    accel_profile=custom 1 0.0 1.0
    # accel_profile=custom 0.5 0.0 1.0
}

# device:kingsis-peripherals-zowie-gaming-mouse {
#     sensitivity = 1.0 # -1.0 - 1.0, 0 means no modification.
#     # force_no_accel = true
#     accel_profile=flat
# }

device {
  name = pnp0c50:00-06cb:cdb0-touchpad

  sensitivity = 0.0
  accel_profile = adaptive
  natural_scroll = yes
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 5
    gaps_out = 15
    border_size = 2
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    # col.inactive_border = rgba(595959aa)
    col.inactive_border = rgba(565a6eaa)

    # Looks terrible, as the yellow of the title-bar seems to be hard-coded
    # col.group_border_active = rgba(bb9af7aa)
    # col.group_border = rgba(16677566)

    # cursor_inactive_timeout = 10
    # no_cursor_warps = true

    layout = dwindle
    # layout = master
}

  #######################
 ## Window Decoration ##
#######################

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 2

    shadow {
      enabled = yes
      range = 4
      render_power = 3
      color = rgba(1a1a1aee)
    }

    blur {
      enabled = yes
        size = 3
        passes = 1
        new_optimizations = one
    }
}


animations {
    enabled = yes

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more
    # bezier = myBezier, 0.05, 0.9, 0.1, 1.05
    #
    # animation = windows, 1, 7, myBezier
    # animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    # animation = fade, 1, 7, default
    # animation = workspaces, 1, 6, default

    # Stolen from Chris Titus
    bezier = overshot, 0.05, 0.9, 0.1, 1.05
    bezier = smoothOut, 0.36, 0, 0.66, -0.56
    bezier = smoothIn, 0.25, 1, 0.5, 1

    animation = windows, 1, 5, overshot, slide
    animation = windowsOut, 1, 4, smoothOut, slide
    animation = windowsMove, 1, 4, default
    animation = border, 1, 10, default
    animation = fade, 1, 10, smoothIn
    animation = fadeDim, 1, 10, smoothIn
    animation = workspaces, 1, 6, default
}

dwindle {
    #no_gaps_when_only = true -> Option no longer exists?

    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
}

master {
    #no_gaps_when_only = true -> Option no longer exists?

    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    new_status = master

    orientation = center
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = true
}

# Additional tweaking
misc {
    # disable_hyprland_logo = true
    # force_hypr_chan = true

    enable_swallow = true
    swallow_regex = ^(kitty)$

    animate_manual_resizes = true
    animate_mouse_windowdragging = false  # Feels laggy

    # Field not found !?
    # background_color = 0xff111111
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
device {
    name = epic-mouse-v1

    sensitivity = -0.5
}

  ##################
 ## Window Rules ##
##################

# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more

windowrule = float, file_progress
windowrule = float, confirm
windowrule = float, dialog
windowrule = float, download
windowrule = float, notification
windowrule = float, error
windowrule = float, splash
windowrule = float, confirmreset
windowrule = float, title:Open File
windowrule = float, title:branchdialog

windowrule = float, blueman-manager
# windowrule = size 50%, blueman-manager
# windowrule = center, blueman-manager
windowrule = float, org.gnome.Calculator

windowrule = float, nm-connection-editor
windowrule = float, title:Vorta for Borg Backup

windowrule = float, pavucontrol-qt
windowrule = float, pavucontrol

windowrule = float, Matplotlib

windowrulev2 = workspace special:keepass, class:^(org\.keepassxc\.KeePassXC)$
windowrulev2 = workspace special:spotify, class:^(Spotify)$

#layerrule = noanim, rofi
#layerrule = blur, rofi

# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER
$scriptDir=~/.config/hypr/scripts


  #####################
 ## Workspace Rules ##
#####################

# workspace = name: mobile, monitor:eDP-1, default:true
# workspace = name: main, monitor:DP-5, default:true
# workspace = name: left, monitor:DP-4, default:true
# workspace = name: right, monitor:DP-2, default:true

workspace = 1, monitor:eDP-1, default:true
workspace = 1, monitor:DP-5, default:true
workspace = 2, monitor:DP-2, default:true
workspace = 3, monitor:DP-4, default:true

  ##############
 ## Keybinds ##
##############

$termCmd = kitty byobu-tmux new

bind = CTRL ALT, T, exec, $termCmd
bind = $mainMod, T, exec, $termCmd
bind = $mainMod, Return, exec, $termCmd
#bind = CTRL, Return, exec, $termCmd

bind = CTRL, Space, exec, rofi -show drun -sync
#bind = CTRL ALT, S, exec, rofi -show ssh -sync
bind = $mainMod, W, exec, rofi -show window -sync
bind = CTRL ALT, F, exec, firefox
bind = CTRL ALT, E, exec, thunar

bind = CTRL ALT, K, exec, $scriptDir/toggle-keepass.sh
bind = CTRL ALT, M, exec, $scriptDir/toggle-spotify.sh
# bind = CTRL ALT, S, exec, spotify

bind = $mainMod, N, exec, $scriptDir/random_wallpaper.sh

bind = $mainMod, Q, killactive, 

bind = $mainMod, space, exec, hyprctl switchxkblayout keyd-virtual-keyboard next

bind = $mainMod, escape, exec, swaylock
bind = $mainMod, Delete, exec, wlogout

# Dwindle-Layout binds
bind = $mainMod, U, fullscreen
bind = $mainMod, I, pseudo
bind = $mainMod, O, togglefloating
bind = $mainMod, P, togglesplit



# Screenshots
bind = CTRL ALT, P, exec, grimblast --notify --freeze copy area
bind = SHIFT ALT, P, exec, grimblast --notify --freeze copy active

# Debugging
bind = $mainMod, X, exec, [float] kitty sh -c "hyprctl activewindow && read -s -N 1 -p 'Press any key to close...'"
bind = $mainMod, C, exec, hyprpicker --autocopy

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
# bind = $mainMod, X, killactive, 
# bind = $mainMod, M, exit, 
# bind = $mainMod, V, togglefloating, 
# bind = $mainMod, R, exec, wofi --show drun


  #####################
 ## Global Keybinds ##
#####################

# Does not work, see https://github.com/keepassxreboot/keepassxc/issues/2281
# bind = CTRL ALT, A, pass, ^(org\.keepassxc\.KeePassXC)$

  #######################
 ## Window management ##
#######################

# Move focus
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d

# Move windows
bind = $mainMod SHIFT, h, movewindow, l
bind = $mainMod SHIFT, l, movewindow, r
bind = $mainMod SHIFT, k, movewindow, u
bind = $mainMod SHIFT, j, movewindow, d

# Resize windows
bind = $mainMod CTRL, h, resizeactive, -20 0
bind = $mainMod CTRL, l, resizeactive, 20 0
bind = $mainMod CTRL, k, resizeactive, 0 -20
bind = $mainMod CTRL, j, resizeactive, 0 20

# Move/resize windows with Mouse
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Grouping
bind= SUPER, G, togglegroup,
bind= SUPER SHIFT, G, moveoutofgroup,
bind= SUPER, tab, changegroupactive,


  ##########################
 ## Workspace management ##
##########################

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Scroll through existing workspaces with mouse (scroll wheel)
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Cycle through workspaces on the current monitor
bind = ALT, tab, workspace, previous
# bind = ALT, tab, workspace, m+1
# bind = ALT SHIFT, tab, workspace, m-1

# Special workspace
bind = $mainMod, grave, togglespecialworkspace
bind = $mainMod SHIFT, grave, movetoworkspace, special

bind = $mainMod, F1, movecurrentworkspacetomonitor, l
bind = $mainMod, F2, movecurrentworkspacetomonitor, ID 2
bind = $mainMod, F3, movecurrentworkspacetomonitor, r
bind = $mainMod, F4, movecurrentworkspacetomonitor, eDP-1


  ##################
 ## Screen setup ##
##################
bindl = $mainMod, F5, exec, $scriptDir/enable_mobile.sh
bindl = $mainMod, F7, exec, $scriptDir/enable_left.sh
bindl = $mainMod, F8, exec, $scriptDir/enable_triple_head.sh


  ################
 ## Media keys ##
################

$volumeLimit = 1.0  # 1.0 = 100%

# bindl = , XF86AudioRaiseVolume, exec, wpctl set-volume -l $volumeLimit @DEFAULT_AUDIO_SINK@ 5%+
# bindl = , XF86AudioLowerVolume, exec, wpctl set-volume -l $volumeLimit @DEFAULT_AUDIO_SINK@ 5%-
# bindl = , XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindl = , XF86AudioRaiseVolume, exec, swayosd-client --output-volume +5
bindl = , XF86AudioLowerVolume, exec, swayosd-client --output-volume -5
bindl = , XF86AudioMute, exec, swayosd-client --output-volume mute-toggle

bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPrev, exec, playerctl previous

# Brightness controls
$brightness_step = 10
# bindl = , XF86MonBrightnessUp, exec, brightnessctl -q set $brightness_step%+
# bindl = , XF86MonBrightnessDown, exec, brightnessctl -q set $brightness_step%-

# Requires brillo and the user to be in the 'video' group
# https://gitlab.com/cameronnemo/brillo/-/blob/trunk/doc/man/brillo.1.md
#
# bindl = , XF86MonBrightnessUp, exec, brillo -u 150000 -A 10
# bindl = CTRL, XF86MonBrightnessUp, exec, brillo -u 150000 -A 2
# bindl = , XF86MonBrightnessDown, exec, brillo -u 150000 -U 10
# bindl = CTRL, XF86MonBrightnessDown, exec, brillo -u 150000 -U 2

bindl = , XF86MonBrightnessUp, exec, swayosd-client --brightness +10
bindl = CTRL, XF86MonBrightnessUp, exec, swayosd-client --brightness +2
bindl = , XF86MonBrightnessDown, exec, swayosd-client --brightness -10
bindl = CTRL, XF86MonBrightnessDown, exec, swayosd-client --brightness -2

# Disable integrated display when lid is closed
bindl = , switch:on:Lid Switch, exec, hyprctl keyword monitor "eDP-1, disable"
bindl = , switch:off:Lid Switch, exec, hyprctl keyword monitor "eDP-1,1920x1080,2560x1440,1"

